# --- Django 核心 ---
Django==5.0.4
asgiref==3.8.1
sqlparse==0.5.0
pytz==2024.1

# --- WebSocket / Channels (必要) ---
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
redis==5.0.4
Twisted==24.3.0
autobahn==23.6.2

# --- 部署與資料庫 (Render 必要) ---
whitenoise==6.6.0
dj-database-url==2.1.0
psycopg2-binary==2.9.9
gunicorn==22.0.0

# --- 應用邏輯 (必要) ---
requests==2.31.0
ollama==0.1.8
OpenCC==1.1.7
numpy==1.26.4
pdfplumber==0.11.0
pypdf==4.2.0
Pillow==10.3.0

# --- Google/Firebase (如果你有用到登入或雲端硬碟才留，沒用到可刪) ---
# firebase-admin==6.5.0
# google-cloud-storage==2.16.0